# Browser -> API (the server's LAN address). Copy to .env.local for local dev.
NEXT_PUBLIC_API_BASE=http://192.168.1.10:8503
# Server components inside Docker reach the api container directly.
API_INTERNAL_BASE=http://api:8503
