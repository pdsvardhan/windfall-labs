"""Deprecated package — intentionally empty.

The homegrown Durability/Valuation/Momentum scoring that once lived here was removed in
ADR-019 (2026-06-21); the platform uses raw fundamentals + Trendlyne's own DVM directly.
The `own_dvm` and `validate` modules no longer exist. This package is kept only as a
namespace placeholder and may be deleted once nothing imports it.
"""

